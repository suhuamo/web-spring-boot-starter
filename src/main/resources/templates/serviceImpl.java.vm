package ${package.ServiceImpl};

##import ${superServiceImplClassPackage};
import com.suhuamo.web.component.service.impl.WebServiceImpl;
import ${package.Entity}.${entity};
import ${package.Entity}.dto.${entity}DTO;
import ${package.Entity}.vo.${entity}VO;
import ${package.Mapper}.${table.mapperName};
import ${package.Service}.${table.serviceName};
import org.springframework.stereotype.Service;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.transaction.annotation.Transactional;
/**
 * <p>
 * $!{table.comment} 服务实现类
 * </p>
 *
 * @author ${author}
 * @slogan 想和喜欢的人睡在冬日的暖阳里
 * @date ${date}
 * 该服务类对应的 pojo 类型对应的 mapper 接口已注入 WebServiceImpl ，名称为 baseMapper
 */
@Service
@Transactional
public class ${table.serviceImplName} extends WebServiceImpl<${table.mapperName}, ${entity}, ${entity}VO, ${entity}DTO> implements ${table.serviceName} {

    /**
    *  向 WebServiceImpl 注入所需的mapper接口和其他内容
    * @param ${table.entityPath}Mapper
    * @return null
    */
    @Autowired
    public ${table.serviceImplName}(${table.mapperName} ${table.entityPath}Mapper) {
        setBaseMapper(${table.entityPath}Mapper);
        setDemoEntity(new ${entity}());
        setDemoEntityVO(new ${entity}VO());
        setDemoEntityDTO(new ${entity}DTO());
    }


}
